import { parseContractPath } from 'typechain';
import { AbiInterface } from './codegen-controller';
export { parseContractPath };
export declare function loadReadAbi(filePath: string): AbiInterface[] | {
    abi: AbiInterface[];
};
