import { Sequelize } from '@subql/x-sequelize';
import { Pool } from 'pg';
import { SUPPORT_DB } from '../../constants';
export declare function getDbType(queryFrom: Sequelize | Pool): Promise<SUPPORT_DB>;
