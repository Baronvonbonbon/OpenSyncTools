"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.stringify = exports.strip = void 0;
const strip = (obj) => {
    var copy;
    // Handle the 3 simple types, and null or undefined
    if (null == obj || 'object' != typeof obj)
        return obj;
    // Handle Date
    if (obj instanceof Date) {
        copy = new Date();
        copy.setTime(obj.getTime());
        return copy;
    }
    // Handle Array
    if (obj instanceof Array) {
        copy = [];
        for (var i = 0, len = obj.length; i < len; i++) {
            copy[i] = (0, exports.strip)(obj[i]);
        }
        return copy;
    }
    // Handle Object
    if (obj instanceof Object || typeof obj === 'object') {
        copy = {};
        for (var attr in obj) {
            if (obj.hasOwnProperty(attr)) {
                switch (attr) {
                    case 'loc':
                    case 'start':
                    case 'end':
                        break;
                    default:
                        // @ts-ignore
                        copy[attr] = (0, exports.strip)(obj[attr]);
                }
            }
            else {
                // @ts-ignore
                copy[attr] = (0, exports.strip)(obj[attr]);
            }
        }
        return copy;
    }
    throw new Error("Unable to copy obj! Its type isn't supported.");
};
exports.strip = strip;
const stringify = (ast, $2 = null, $3 = 2) => {
    const obj = (0, exports.strip)(ast);
    return JSON.stringify(obj, $2, $3);
};
exports.stringify = stringify;
exports.default = exports.stringify;
//# sourceMappingURL=index.js.map