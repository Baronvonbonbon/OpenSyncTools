/** Globals provided by the SubQuery runtime (legacy/global-store style) */
declare const store: any;   // used by generated model classes
declare const api: any;     // (already declared elsewhere, harmless duplicate)
declare const logger: any;  // (already declared elsewhere, harmless duplicate)
